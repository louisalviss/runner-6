name: Runner 6 DSH Secure V2 (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Production uses dsh-secure.yml (V3)."
